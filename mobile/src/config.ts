// Change this to your PC's LAN IP (or Tailscale hostname) when testing on a real device.
// Example: "http://192.168.1.42:5000"  or  "http://100.x.x.x:5000" (Tailscale)
// Use HTTP for local dev to avoid self-signed cert issues.
export const SERVER_URL = "http://10.227.84.74:5000";
